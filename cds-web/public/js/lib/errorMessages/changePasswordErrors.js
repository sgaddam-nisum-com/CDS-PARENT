define(['jquery'], function($) {

    return {

        "password-eq": "Confirm password field should be matched with password",
        "newPassword-required": "Password is a required field",
        "newPassword-minCharacters": "Password should contain minimum 6 characters"
    }

});
